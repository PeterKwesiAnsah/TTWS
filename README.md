# Tiny

Tiny is a minimal iterative HTTP/1.1 web server written in C that serves both static and dynamic content.
It uses `libpq` to connect to a PostgreSQL-compatible database and exposes simple(CRUD) user management URIs.
